# Security

Report security-sensitive issues privately to maintainers rather than opening a public issue.

Principles:
- least privilege for tools/capabilities
- no secrets in benchmark fixtures
- learned routers cannot bypass deterministic policy gates
- external model/tool outputs are untrusted until validated
- evidence records avoid sensitive payloads by default
